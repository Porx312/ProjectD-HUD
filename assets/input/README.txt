Input device icons for input_type (wheel | controller | keyboard).

Files:
- wheel.png       — steering wheel
- controller.png  — gamepad
- keyboard.png    — keyboard

Shown in profile (next to lap time) and competition rows (left of best lap).

Regenerate:
  python3 scripts/generate-input-icons.py
